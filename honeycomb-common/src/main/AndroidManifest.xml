<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.honeycomb">

    <application>
        <provider
            android:name=".provider.HoneycombInitProvider"
            android:authorities="${applicationId}.google.honeycombinitprovider"
            android:exported="false"
            android:initOrder="100" />
    </application>
</manifest>
